class User {
  constructor(id, studentId, balance, email, fullname, avatar) {
    this.id = id;
    this.studentId = studentId;
    this.balance = balance;
    this.email = email;
    this.fullname = fullname;
    this.avatar = avatar;
  }
}

export default User;
